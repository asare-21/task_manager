import { FractionalPercent__Output } from "./generated/envoy/type/v3/FractionalPercent";
export interface Fraction {
    numerator: number;
    denominator: number;
}
export declare function fractionToString(fraction: Fraction): string;
export declare function envoyFractionToFraction(envoyFraction: FractionalPercent__Output): Fraction;
